# Submission log

One folder per portal upload (5 per day, 15 total).

- `matching_results/` — `matching_results.tsv` scored on the leaderboard
- `code/` — code zip uploaded with that submission

Naming: `Submission{N}_Day{D}` (e.g. `Submission1_Day1`).
